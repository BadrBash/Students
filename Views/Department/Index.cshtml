@model List<PracticeMVC.Models.DepartmentViewModel>

@{
    ViewData["Title"] = "Department List";
}

<div>
    <a href="@Url.Action("Create", "Department")" class="btn btn-primary">Create New Student</a>
</div>

@if (Model.Count > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Description</td>
                    <td>
                        <a href="@Url.Action("Edit", "Department", new {Id = @item.Id})" class="btn btn-primary">Edit</a>
                        <form asp-action="Delete" asp-controller="Student" asp-route-id="@item.Id" style="display:inline-block">
                            <input type="submit" value="Delete" class="btn btn-danger">
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p class="text-danger">No Departments found!</p>
}